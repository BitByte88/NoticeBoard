<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.noticeBoard.dao.mapper.LoginMapper">

    <!-- 管理者情報取得 -->
    <select id="getUserInfo" resultType="jp.co.noticeBoard.dto.UserDto">
        SELECT
<!--         M_MANAGE_USER.user_id AS userId, -->
        M_MANAGE_USER.password AS password,
        M_MANAGE_USER.name AS name,
        M_MANAGE_USER.user_no AS userNo
        FROM
        M_MANAGE_USER
        WHERE
        M_MANAGE_USER.user_id= #{account} AND
        M_MANAGE_USER.delete_flag=0
    </select>

</mapper>